A minimalist recreation of Git in Rust


commands:

```
cargo run -- init
```

```
cargo run -- add README.md
```

``` 
cargo run -- cat-file -p [your-hash]
```

``` 
cargo run -- cat-file -p [your-hash]
```
``` 
cargo run -- diff [old-hash] [new-hash]
``` 